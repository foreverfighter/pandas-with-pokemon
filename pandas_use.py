# there are 3 main objects to know, SERIES, DATAFRAMES, NDARRAYS(ARRAYS)
s = pd.Series([1, 2, 3])
s = pd.Series([1, 2, 3], index=['a', 'b', 'c'])

a = np.array([1, 2, 3])
a2d = np.array([[1, 2], [3, 4]])
